# ----------------------------------------------------------------
# ---- Generic IRC routes (not tied to a channel nor network) ----
# ----------------------------------------------------------------
search:
    path: /search/
    defaults:
        _controller: korobi.controller.irc.search:searchAction
channel_autocomplete:
    path: /search/autocomplete
    defaults:
        _controller: korobi.controller.irc.search:autocompleteAction
