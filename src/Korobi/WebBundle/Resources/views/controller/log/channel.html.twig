{% extends 'KorobiWebBundle::layout.html.twig' %}

{% set page_title = "Log" %}

{% block body %}
    <div class="logs">
        {% for message in logs %}
            <span class="logs--line">{{ message|raw }}</span>
        {% endfor %}
    </div>
{% endblock %}
