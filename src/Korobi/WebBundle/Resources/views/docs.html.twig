{% extends 'KorobiWebBundle::layout.html.twig' %}

{% set page_title = "Docs" %}

{% block body %}
    {% if pageName != "index" %}
        <a href="{{ path('docs_index') }}">Return to docs index</a>
    {% endif %}
    <section class="docs">
        {{ content | raw }}
    </section>
{% endblock %}
