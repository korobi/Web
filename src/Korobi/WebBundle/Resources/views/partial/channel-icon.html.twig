{% if key == null %}
    <i class="private-channel-icon {% if inline is defined and inline %}inline{% endif %} {% if super is defined and super %}super{% endif %} fa fa-lock" title="Private"></i>
{% endif %}