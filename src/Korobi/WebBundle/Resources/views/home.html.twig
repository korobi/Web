{% extends 'KorobiWebBundle::layout.html.twig' %}

{% set page_title = "Home" %}

{% block body %}
    Welcome home, darling. I won't believe it until I see it!

    {% for i in 0..2 %}
        {% for x in 0..2 %}
            <p class="irc--{{ i }}-{{ x }}">Hi there. I'm an IRC colour. I have foreground {{ i }} and background {{ x }}. I'm <b>not</b> inverted.</p>
            <p class="irc--{{ i }}-{{ x }}-inverted">Hi there. I'm an IRC colour. I have foreground {{ i }} and background {{ x }}. I <b>am</b> inverted.</p>
        {% endfor %}
    {% endfor %}
{% endblock %}
